import Button from "./Button"

const Header = () => {
    return (
        <div>
            <header className='header'>
                <h1>Tracker</h1>
                < Button />
            </header>
        </div>
    )
}

export default Header
